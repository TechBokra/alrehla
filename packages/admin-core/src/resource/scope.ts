/** Alrehla has a single application scope; this hook is intentionally free of tenant/commerce concepts. */
export type ResourceScope = 'global';
export const DEFAULT_RESOURCE_SCOPE: ResourceScope = 'global';
