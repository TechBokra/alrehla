export const initGA = () => {
  // Deprecated: Google Analytics 4 is now integrated at the root layout via @next/third-parties/google.
};

export const pageview = (_url: string) => {
  // Deprecated: Google Analytics 4 automatically handles page views via Next.js App Router integration.
};
