/**
 * Compatibility entry point for callers that still import the legacy page view.
 * The route and all new code use the feature resource directly.
 */
export { UsersResourcePage as default } from '../../features/users';
